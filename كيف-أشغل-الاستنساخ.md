# كيف أشغّل الاستنساخ الصوتي؟

## الوضع باختصار

الدبلجة **جاهزة الآن** بأصوات استوديو مصرية:
`projects/Phantom-Thread/output/Phantom-Thread.mp4`

الاستنساخ — أي أن ينطق العربيةَ **صوتُ الممثل الأصلي نفسه** — يحتاج ملفات
نموذج من موقع HuggingFace. هذا الموقع محجوب في بيئة العمل هنا، لكنه **مفتوح
على خوادم GitHub**. لذلك يُشغَّل الاستنساخ هناك.

---

## الطريقة: ثلاث خطوات على موقع GitHub

لا تحتاج تثبيت أي برنامج. كل شيء من المتصفح.

### الخطوة ١ — فعّل ملف التشغيل

الملف جاهز لكنه في مجلد القوالب، لأن صلاحياتي لا تسمح بوضعه في مساره النهائي.

افتح المستودع على GitHub، ثم:

1. اذهب إلى `docs/github-actions/clone.yml`
2. اضغط أيقونة **✏️ Edit** ثم انسخ كل المحتوى
3. اضغط **Add file → Create new file**
4. اكتب في خانة الاسم بالضبط:
   ```
   .github/workflows/clone.yml
   ```
5. الصق المحتوى، ثم **Commit changes**

> تأكد أنك على الفرع `arena/01a03969-prostudio`.

### الخطوة ٢ — شغّل الاستنساخ

1. افتح تبويب **Actions** في أعلى الصفحة
2. اختر **Clone voices and dub** من القائمة اليمنى
3. اضغط **Run workflow** (الزر الرمادي على اليمين)
4. اترك الخيارات كما هي واضغط **Run workflow** الأخضر

المدة المتوقعة: **٢٠ إلى ٤٠ دقيقة** (تنزيل النموذج ثم توليد ٢٤ جملة).

### الخطوة ٣ — حمّل النتيجة

عند انتهاء التشغيل (علامة ✅):

1. اضغط على اسم التشغيلة
2. انزل إلى قسم **Artifacts** في أسفل الصفحة
3. حمّل **cloned-dub-Phantom-Thread**

داخل الملف المضغوط:

| الملف | المحتوى |
| --- | --- |
| `output/Phantom-Thread.mp4` | الفيديو المدبلج بصوت الممثل |
| `output/Phantom-Thread.srt` | الترجمة |
| `voices/*.wav` | التسجيلات المستنسخة، جملة جملة |

---

## ماذا يفعل الاستنساخ بالضبط؟

1. يفصل حوار الممثلين عن الموسيقى في المقطع الأصلي
2. يقتطع من صوت **كل ممثل** أنظف مقطع من جمله هو
3. يولّد جمله العربية بصوته باستخدام F5-TTS
4. يركّب الدبلجة ويرفعها

التسجيلات الحالية تُنسخ إلى `voices_before_clone/` قبل الاستبدال، فإن لم تعجبك
النتيجة يمكن الرجوع عنها.

---

## إن أردت تشغيله على جهازك بدل GitHub

يصلح على أي جهاز يصل إلى الإنترنت بحرية:

```bash
git clone -b arena/01a03969-prostudio https://github.com/dhiyaddineb-hue/prostudio.git
cd prostudio
pip install -r requirements.txt f5-tts

PROJECT=Phantom-Thread python scripts/clone_project.py
PROJECT=Phantom-Thread python scripts/build_phantom_dub.py
```

النتيجة في `projects/Phantom-Thread/output/`.

---

## إن فشل التشغيل

| الرسالة | السبب والحل |
| --- | --- |
| `F5-TTS weights are not available` | الشبكة تحجب HuggingFace — جرّب GitHub Actions |
| `no source clip` | ضع فيديو في `inbox/` أو في `projects/<الاسم>/source/` |
| `no cues in project.json` | المشروع بلا نص — راجع `project.json` |

المشروع لا يتغيّر عند الفشل: تبقى تسجيلاتك الحالية كما هي.

---

## ملاحظة على التراخيص

أوزان بعض نماذج الاستنساخ مرخّصة **CC BY-NC** أي **غير تجارية**. نموذج F5-TTS
المستخدم هنا مرخّص بـ CC BY-NC 4.0 — مناسب للاستخدام الشخصي، وراجع الترخيص
إن نويت استخداماً تجارياً.
