# ذاكرة المشروع — ProStudio

## قاعدة العمل الدائمة

يجب اعتبار `arena/01a03969-prostudio` الفرع الدائم الذي اختاره المستخدم لهذا المشروع. قبل أي تعديل، تحقّق من أن `git branch --show-current` يعرض هذا الاسم، ولا تستبدله بفرع جلسة أو فرع آخر من تلقاء نفسك. GitHub هو المشغّل والبيئة، والوكيل يقرأ المشروع ويصدر أوامر التعديل له.

## الهوية والروابط

المستودع هو [dhiyaddineb-hue/prostudio](https://github.com/dhiyaddineb-hue/prostudio). صفحة GitHub Pages المنشورة من مجلد `docs/` على الفرع الدائم هي [https://dhiyaddineb-hue.github.io/prostudio/](https://dhiyaddineb-hue.github.io/prostudio/)، وصفحة إرسال الملفات هي [https://dhiyaddineb-hue.github.io/prostudio/send.html](https://dhiyaddineb-hue.github.io/prostudio/send.html).

## ما تم فهمه من المراجعة

ProStudio استوديو عربي لدبلجة وترجمة فيديوهات YouTube والملفات المحلية. مساره الأساسي هو التنزيل أو الرفع، التفريغ، الترجمة، توليد الصوت، مزامنة الصوت ومزج الخلفية، ثم تصدير MP4 وملفات الترجمة. النواة مكتوبة Python داخل `youtube_auto_dub/`، وواجهة التشغيل المحلية FastAPI داخل `web/` وتعمل افتراضياً على المنفذ 8080، بينما واجهة GitHub Pages ثابتة داخل `docs/` وتعرض الدبلجات الجاهزة وتوفر الرفع المباشر ومصمم الأصوات.

المجلدات الأساسية هي: `youtube_auto_dub/` للنواة، `web/` للخادم والواجهة المحلية، `docs/` لصفحة GitHub Pages، `projects/` لمشاريع الدبلجة المستقلة، `scripts/` لأدوات البناء والنشر، `tests/` للاختبارات، و`inbox/` للملفات الواردة. تمت مراجعة README ومذكرة المشروع وخريطة الملفات وإعدادات الفرع قبل تسجيل هذه الذاكرة.

## أرقام مرجعية من آخر مراجعة

يحتوي المشروع على 30 وحدة Python في النواة، و11 ملفاً في الواجهة المحلية، و13 سكربتاً، و31 ملف اختبار، وأربعة مشاريع دبلجة: Bob-Proctor-5min-2v وBob-Proctor-Sample وPhantom-Thread وVikings-Ragnar-Floki.

## حدود طلب اللعبة الحالي

الرسالة الحالية تطلب فهم المشروع وتثبيت الفرع والروابط في الذاكرة، لكنها لا تقدم تصميم لعبة أو آليات لعب أو هدفاً بصرياً. لذلك لا ينبغي إنشاء لعبة أو تحويل المشروع إلى Babylon/WebDev دون وصف إضافي صريح؛ عند وصول وصف لعبة لاحقاً، يجب أولاً قراءة مهارة تطوير الألعاب ثم بناءها في نطاق منفصل دون كسر خط الدبلجة الحالي.
